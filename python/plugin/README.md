# StarPC Python generator plugin

`starpc-python-plugin` provides the `protoc-gen-starpc-python` executable. It
reads protoc requests and generates typed Python clients, service
registrations, and interface stubs for StarPC services.

Install the `starpc` distribution in applications that import generated
modules. Install this plugin only in environments that generate code.
